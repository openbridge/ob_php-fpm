# language setting
language: none

pre_ci:
      - docker build -t openbridge/php-fpm:latest
build:
    post_ci:
      - sudo pip install -U docker-compose
      - docker-compose up -d
      - docker ps
